int main() {
  decl
    int temp;
  enddecl

  begin
    temp = exposcall("Fork");
    if(temp != 0) then
      temp = exposcall("Wait", temp);
    endif;
    temp = exposcall("Fork");
    if(temp != 0) then
      temp = exposcall("Wait", temp);
    endif;
    temp = exposcall("Fork");
    if(temp != 0) then
      temp = exposcall("Wait", temp);
    endif;
    temp = exposcall("Exec", "pid.xsm");
    return 0;
  end
}